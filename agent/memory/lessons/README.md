# Lessons Learned (per area)

One file per area: `backend.md`, `frontend.md`, `git.md`, `qa.md`, `infra.md`,
`process.md`. Agents READ the relevant area file before starting a task;
the retro WRITES here (skills/retro). Recurring lessons get promoted:
lesson → rule (SKILL.md edit, human-approved) → hook (deterministic).

Scope: lessons here belong to THIS project's repo. Mark each entry's
"applies to" as `this project` or `all projects` — `all projects` entries
are promotion candidates for the template (`/harness-sync promote`), so
every future project inherits them as seed lessons.

Entry format (append, never rewrite history):
