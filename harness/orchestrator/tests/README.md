# orchestrator tests

Focused standard-library regression tests for deterministic harness behavior.

Run:

```sh
python3 -m unittest discover -s harness/orchestrator/tests -v
```

These tests cover orchestration policy, not product code or generated project
artifacts.
