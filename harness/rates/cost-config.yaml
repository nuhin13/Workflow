# Per-model rate card (USD per MILLION tokens) used by metrics/dashboard.
# ⚠️ Prices move monthly — verify against vendor pricing pages before
# budgeting. cache_read is typically ~10% of input; cache_write ~125%.
updated: "2026-06"
models:
  claude-opus:    { input: 5.00,  output: 25.00, cache_read: 0.50,  cache_write: 6.25 }
  claude-sonnet:  { input: 3.00,  output: 15.00, cache_read: 0.30,  cache_write: 3.75 }
  claude-haiku:   { input: 1.00,  output: 5.00,  cache_read: 0.10,  cache_write: 1.25 }
  gpt-codex:      { input: 1.75,  output: 14.00 }
  gemini-pro:     { input: 2.00,  output: 12.00 }
  kimi-k2:        { input: 0.60,  output: 2.50 }
aliases:   # map raw model strings from run JSON → rate keys (prefix match)
  claude-opus: claude-opus
  claude-sonnet: claude-sonnet
  claude-haiku: claude-haiku
  gpt: gpt-codex
  gemini: gemini-pro
  kimi: kimi-k2
