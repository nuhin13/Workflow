/* tslint:disable */
/* eslint-disable */
/**
 * Garazo API
 * Canonical Garazo application contract (ADR-0008). This file is the single source of truth for every HTTP surface; the Dart and TypeScript clients are GENERATED from it and are never hand-edited.  E00 defines infrastructure routes only. No product entity, payload, or authorization surface is authorized here — those arrive with their own epics. A workshop identifier supplied by a client is NEVER authorization (NFR-SEC-01). 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type ErrorEnvelope,
    ErrorEnvelopeFromJSON,
    ErrorEnvelopeToJSON,
} from '../models/ErrorEnvelope';
import {
    type WorkshopSetupRequest,
    WorkshopSetupRequestFromJSON,
    WorkshopSetupRequestToJSON,
} from '../models/WorkshopSetupRequest';
import {
    type WorkshopSummary,
    WorkshopSummaryFromJSON,
    WorkshopSummaryToJSON,
} from '../models/WorkshopSummary';

export interface SetUpCurrentWorkshopRequest {
    workshopSetupRequest: WorkshopSetupRequest;
}

/**
 * 
 */
export class WorkshopsApi extends runtime.BaseAPI {

    /**
     * Creates request options for getCurrentWorkshop without sending the request
     */
    async getCurrentWorkshopRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/workshops/current`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * \"Current\" means the workshop this session is authorized for, resolved entirely server-side. There is no request parameter that names a workshop, so a client cannot name someone else\'s (`NFR-SEC-01`). 
     * Read the workshop resolved for the current session
     */
    async getCurrentWorkshopRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkshopSummary>> {
        const requestOptions = await this.getCurrentWorkshopRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkshopSummaryFromJSON(jsonValue));
    }

    /**
     * \"Current\" means the workshop this session is authorized for, resolved entirely server-side. There is no request parameter that names a workshop, so a client cannot name someone else\'s (`NFR-SEC-01`). 
     * Read the workshop resolved for the current session
     */
    async getCurrentWorkshop(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkshopSummary> {
        const response = await this.getCurrentWorkshopRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for setUpCurrentWorkshop without sending the request
     */
    async setUpCurrentWorkshopRequestOpts(requestParameters: SetUpCurrentWorkshopRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['workshopSetupRequest'] == null) {
            throw new runtime.RequiredError(
                'workshopSetupRequest',
                'Required parameter "workshopSetupRequest" was null or undefined when calling setUpCurrentWorkshop().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/workshops/current/setup`;

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: WorkshopSetupRequestToJSON(requestParameters['workshopSetupRequest']),
        };
    }

    /**
     * Idempotent by method, but only ever succeeds once per account: a second call returns `WORKSHOP.ALREADY_SET_UP`. BD region profile and the default Bangla locale are assigned by the access domain, not by this request. 
     * Complete the one-time initial workshop setup
     */
    async setUpCurrentWorkshopRaw(requestParameters: SetUpCurrentWorkshopRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkshopSummary>> {
        const requestOptions = await this.setUpCurrentWorkshopRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkshopSummaryFromJSON(jsonValue));
    }

    /**
     * Idempotent by method, but only ever succeeds once per account: a second call returns `WORKSHOP.ALREADY_SET_UP`. BD region profile and the default Bangla locale are assigned by the access domain, not by this request. 
     * Complete the one-time initial workshop setup
     */
    async setUpCurrentWorkshop(requestParameters: SetUpCurrentWorkshopRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkshopSummary> {
        const response = await this.setUpCurrentWorkshopRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
