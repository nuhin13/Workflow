/* tslint:disable */
/* eslint-disable */
/**
 * Garazo API
 * Canonical Garazo application contract (ADR-0008). This file is the single source of truth for every HTTP surface; the Dart and TypeScript clients are GENERATED from it and are never hand-edited.  E00 defines infrastructure routes only. No product entity, payload, or authorization surface is authorized here — those arrive with their own epics. A workshop identifier supplied by a client is NEVER authorization (NFR-SEC-01). 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type ErrorEnvelope,
    ErrorEnvelopeFromJSON,
    ErrorEnvelopeToJSON,
} from '../models/ErrorEnvelope';
import {
    type IdentityAssertionRequest,
    IdentityAssertionRequestFromJSON,
    IdentityAssertionRequestToJSON,
} from '../models/IdentityAssertionRequest';
import {
    type LocalePreference,
    LocalePreferenceFromJSON,
    LocalePreferenceToJSON,
} from '../models/LocalePreference';
import {
    type OwnerPinRecoveryRequest,
    OwnerPinRecoveryRequestFromJSON,
    OwnerPinRecoveryRequestToJSON,
} from '../models/OwnerPinRecoveryRequest';
import {
    type OwnerPinSetRequest,
    OwnerPinSetRequestFromJSON,
    OwnerPinSetRequestToJSON,
} from '../models/OwnerPinSetRequest';
import {
    type OwnerPinVerifyRequest,
    OwnerPinVerifyRequestFromJSON,
    OwnerPinVerifyRequestToJSON,
} from '../models/OwnerPinVerifyRequest';
import {
    type OwnerPinVerifyResponse,
    OwnerPinVerifyResponseFromJSON,
    OwnerPinVerifyResponseToJSON,
} from '../models/OwnerPinVerifyResponse';
import {
    type SessionExchangeResponse,
    SessionExchangeResponseFromJSON,
    SessionExchangeResponseToJSON,
} from '../models/SessionExchangeResponse';

export interface ExchangeIdentityAssertionRequest {
    identityAssertionRequest: IdentityAssertionRequest;
}

export interface RecoverOwnerPinRequest {
    ownerPinRecoveryRequest: OwnerPinRecoveryRequest;
}

export interface SetLocalePreferenceRequest {
    localePreference: LocalePreference;
}

export interface SetOwnerPinRequest {
    ownerPinSetRequest: OwnerPinSetRequest;
}

export interface VerifyOwnerPinRequest {
    ownerPinVerifyRequest: OwnerPinVerifyRequest;
}

/**
 * 
 */
export class AccessApi extends runtime.BaseAPI {

    /**
     * Creates request options for exchangeIdentityAssertion without sending the request
     */
    async exchangeIdentityAssertionRequestOpts(requestParameters: ExchangeIdentityAssertionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['identityAssertionRequest'] == null) {
            throw new runtime.RequiredError(
                'identityAssertionRequest',
                'Required parameter "identityAssertionRequest" was null or undefined when calling exchangeIdentityAssertion().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/api/v1/access/sessions`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: IdentityAssertionRequestToJSON(requestParameters['identityAssertionRequest']),
        };
    }

    /**
     * Verifies the assertion through the server\'s `PhoneIdentityPort` and rotates the Garazo application session: this call always issues a NEW session, and any previous session for the account stops being valid. Not idempotent.  `workshop` is `null` when the account has completed no setup yet. That is how a client knows to show first-time setup rather than the dashboard — it is never signalled as a 404.  Every failure of this exchange — unknown number, wrong code, expired assertion — returns the SAME `AUTH.INVALID_CREDENTIALS` response, so this endpoint cannot be used to test whether a phone number is a registered Garazo account (`EARS-E01-T01-3`, `L-auth-003`). 
     * Exchange a Firebase phone-identity assertion for a Garazo session
     */
    async exchangeIdentityAssertionRaw(requestParameters: ExchangeIdentityAssertionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SessionExchangeResponse>> {
        const requestOptions = await this.exchangeIdentityAssertionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SessionExchangeResponseFromJSON(jsonValue));
    }

    /**
     * Verifies the assertion through the server\'s `PhoneIdentityPort` and rotates the Garazo application session: this call always issues a NEW session, and any previous session for the account stops being valid. Not idempotent.  `workshop` is `null` when the account has completed no setup yet. That is how a client knows to show first-time setup rather than the dashboard — it is never signalled as a 404.  Every failure of this exchange — unknown number, wrong code, expired assertion — returns the SAME `AUTH.INVALID_CREDENTIALS` response, so this endpoint cannot be used to test whether a phone number is a registered Garazo account (`EARS-E01-T01-3`, `L-auth-003`). 
     * Exchange a Firebase phone-identity assertion for a Garazo session
     */
    async exchangeIdentityAssertion(requestParameters: ExchangeIdentityAssertionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SessionExchangeResponse> {
        const response = await this.exchangeIdentityAssertionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getCurrentSession without sending the request
     */
    async getCurrentSessionRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/access/session`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Restore the current authorized session and workshop context
     */
    async getCurrentSessionRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SessionExchangeResponse>> {
        const requestOptions = await this.getCurrentSessionRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SessionExchangeResponseFromJSON(jsonValue));
    }

    /**
     * Restore the current authorized session and workshop context
     */
    async getCurrentSession(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SessionExchangeResponse> {
        const response = await this.getCurrentSessionRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for recoverOwnerPin without sending the request
     */
    async recoverOwnerPinRequestOpts(requestParameters: RecoverOwnerPinRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['ownerPinRecoveryRequest'] == null) {
            throw new runtime.RequiredError(
                'ownerPinRecoveryRequest',
                'Required parameter "ownerPinRecoveryRequest" was null or undefined when calling recoverOwnerPin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/access/owner-pin/recoveries`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OwnerPinRecoveryRequestToJSON(requestParameters['ownerPinRecoveryRequest']),
        };
    }

    /**
     * Atomically verifies `recoveryAssertion` and sets `newPin` in one step — there is no separate \"verify then set\" round trip, so there is nothing to race. An invalid or expired assertion leaves the existing PIN unchanged (`FR-ACCESS-14`). Success resets the next failure-cycle cooldown to 60 seconds (`FR-ACCESS-15`).  Throttled independently of the PIN failure cycle so recovery cannot be used to bypass PIN cooldown (`Q-009`): at most 5 OTP sends per registered phone per rolling hour, each OTP valid 5 minutes, at most 5 verify attempts per OTP. Exceeding either limit returns `AUTH.RECOVERY_COOLDOWN` and never reveals whether the phone is registered. 
     * Verify a registered-phone OTP assertion and set a new PIN
     */
    async recoverOwnerPinRaw(requestParameters: RecoverOwnerPinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.recoverOwnerPinRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Atomically verifies `recoveryAssertion` and sets `newPin` in one step — there is no separate \"verify then set\" round trip, so there is nothing to race. An invalid or expired assertion leaves the existing PIN unchanged (`FR-ACCESS-14`). Success resets the next failure-cycle cooldown to 60 seconds (`FR-ACCESS-15`).  Throttled independently of the PIN failure cycle so recovery cannot be used to bypass PIN cooldown (`Q-009`): at most 5 OTP sends per registered phone per rolling hour, each OTP valid 5 minutes, at most 5 verify attempts per OTP. Exceeding either limit returns `AUTH.RECOVERY_COOLDOWN` and never reveals whether the phone is registered. 
     * Verify a registered-phone OTP assertion and set a new PIN
     */
    async recoverOwnerPin(requestParameters: RecoverOwnerPinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.recoverOwnerPinRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for revokeCurrentSession without sending the request
     */
    async revokeCurrentSessionRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/access/session`;

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Revoke the current application session
     */
    async revokeCurrentSessionRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.revokeCurrentSessionRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Revoke the current application session
     */
    async revokeCurrentSession(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.revokeCurrentSessionRaw(initOverrides);
    }

    /**
     * Creates request options for revokeOwnerGrant without sending the request
     */
    async revokeOwnerGrantRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/access/owner-pin/grant`;

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Explicit lock (`FR-ACCESS-07`). Always succeeds when the session is valid, whether or not a grant was currently active. 
     * Explicitly revoke the current owner-money grant
     */
    async revokeOwnerGrantRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.revokeOwnerGrantRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Explicit lock (`FR-ACCESS-07`). Always succeeds when the session is valid, whether or not a grant was currently active. 
     * Explicitly revoke the current owner-money grant
     */
    async revokeOwnerGrant(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.revokeOwnerGrantRaw(initOverrides);
    }

    /**
     * Creates request options for setLocalePreference without sending the request
     */
    async setLocalePreferenceRequestOpts(requestParameters: SetLocalePreferenceRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['localePreference'] == null) {
            throw new runtime.RequiredError(
                'localePreference',
                'Required parameter "localePreference" was null or undefined when calling setLocalePreference().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/access/preferences/locale`;

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: LocalePreferenceToJSON(requestParameters['localePreference']),
        };
    }

    /**
     * Changes presentation only. No stored business value changes when the locale changes (`NFR-I18N-01`–`03`). 
     * Persist the Bangla/English presentation preference
     */
    async setLocalePreferenceRaw(requestParameters: SetLocalePreferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LocalePreference>> {
        const requestOptions = await this.setLocalePreferenceRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LocalePreferenceFromJSON(jsonValue));
    }

    /**
     * Changes presentation only. No stored business value changes when the locale changes (`NFR-I18N-01`–`03`). 
     * Persist the Bangla/English presentation preference
     */
    async setLocalePreference(requestParameters: SetLocalePreferenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LocalePreference> {
        const response = await this.setLocalePreferenceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for setOwnerPin without sending the request
     */
    async setOwnerPinRequestOpts(requestParameters: SetOwnerPinRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['ownerPinSetRequest'] == null) {
            throw new runtime.RequiredError(
                'ownerPinSetRequest',
                'Required parameter "ownerPinSetRequest" was null or undefined when calling setOwnerPin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/access/owner-pin`;

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: OwnerPinSetRequestToJSON(requestParameters['ownerPinSetRequest']),
        };
    }

    /**
     * Idempotent by method. Establishing a PIN for the first time needs no `recoveryAssertion`; replacing an existing PIN without a valid session step-up requires one (`recoveryAssertion`), verified the same way `POST .../recoveries` verifies it. 
     * Establish the initial owner PIN, or replace it with recovery proof
     */
    async setOwnerPinRaw(requestParameters: SetOwnerPinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.setOwnerPinRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Idempotent by method. Establishing a PIN for the first time needs no `recoveryAssertion`; replacing an existing PIN without a valid session step-up requires one (`recoveryAssertion`), verified the same way `POST .../recoveries` verifies it. 
     * Establish the initial owner PIN, or replace it with recovery proof
     */
    async setOwnerPin(requestParameters: SetOwnerPinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.setOwnerPinRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for verifyOwnerPin without sending the request
     */
    async verifyOwnerPinRequestOpts(requestParameters: VerifyOwnerPinRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['ownerPinVerifyRequest'] == null) {
            throw new runtime.RequiredError(
                'ownerPinVerifyRequest',
                'Required parameter "ownerPinVerifyRequest" was null or undefined when calling verifyOwnerPin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("sessionToken", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/access/owner-pin/verifications`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OwnerPinVerifyRequestToJSON(requestParameters['ownerPinVerifyRequest']),
        };
    }

    /**
     * Not idempotent — each call consumes one attempt of the current failure cycle (`Q-005`). Success resets the next cooldown to 60 seconds (`FR-ACCESS-15`).  The FIFTH wrong attempt of a cycle returns 429 `AUTH.PIN_COOLDOWN`, not 401: the cooldown starts on that attempt, so the response that starts it must already carry `retryAfterSeconds` (`EARS-E01-T01` correction, aligns with `T05`). Attempts 1–4 return 401 `AUTH.PIN_INVALID` with the `X-Pin-Remaining-Attempts` header. 
     * Verify the owner PIN and issue the separate owner-money grant
     */
    async verifyOwnerPinRaw(requestParameters: VerifyOwnerPinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OwnerPinVerifyResponse>> {
        const requestOptions = await this.verifyOwnerPinRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OwnerPinVerifyResponseFromJSON(jsonValue));
    }

    /**
     * Not idempotent — each call consumes one attempt of the current failure cycle (`Q-005`). Success resets the next cooldown to 60 seconds (`FR-ACCESS-15`).  The FIFTH wrong attempt of a cycle returns 429 `AUTH.PIN_COOLDOWN`, not 401: the cooldown starts on that attempt, so the response that starts it must already carry `retryAfterSeconds` (`EARS-E01-T01` correction, aligns with `T05`). Attempts 1–4 return 401 `AUTH.PIN_INVALID` with the `X-Pin-Remaining-Attempts` header. 
     * Verify the owner PIN and issue the separate owner-money grant
     */
    async verifyOwnerPin(requestParameters: VerifyOwnerPinRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OwnerPinVerifyResponse> {
        const response = await this.verifyOwnerPinRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
