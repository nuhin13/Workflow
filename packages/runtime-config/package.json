{
  "name": "@garazo/runtime-config",
  "version": "0.0.0",
  "private": true,
  "description": "Validated runtime configuration and the redacted structured logger shared by the Garazo API and worker.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
