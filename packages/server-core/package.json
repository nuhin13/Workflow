{
  "name": "@garazo/server-core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared modular-monolith domain/application code (ADR-0002). Owns module boundaries and provider-neutral ports; no adapters, no HTTP.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/pg": "^8.20.3",
    "typescript": "5.9.3"
  }
}
