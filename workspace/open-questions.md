# Open Questions — Garazo

Central register (`workspace/open-questions.md`). Every `Q-###` in any artifact
is indexed here. Blocking questions are mirrored as IDs only in
`workspace/state.yaml`.

| ID | Question | Raised in | Blocks | Options + recommendation | Status | Answer |
|---|---|---|---|---|---|---|
| Q-001 | BRD v1 references `Garazo_Feature_Specs.md`, `Garazo_Design_Brief.md`, and `Garage_Market_Problem_Analysis.md`, but they are not in the repository. Will these be supplied, or are BRD v1 and HTML prototype v1 the complete planning inputs? | Kickoff input review | `/prd` | Supply them if they contain requirements not captured in the BRD or prototype; otherwise explicitly approve BRD v1 + HTML v1 as the complete source set. Recommend supplying them when available to avoid losing product detail. | answered | BRD v1 and HTML prototype v1 are the complete, locked planning inputs. The named companion documents are not part of v1. Any addition, removal, or content change creates v2. Applied to `workspace/plan/00-business/idea.md` and `workspace/state.yaml` on 2026-07-26. |
| Q-002 | May BRD v2 be created as a structural normalization of locked BRD v1, adding the required `BR-###` IDs, template metadata, and Handoff block without changing business meaning or scope? | `/prd` precondition review | `/prd` | A: create BRD v2 and preserve v1 unchanged; B: keep the non-canonical BRD and create a supplemental ID map. Recommend A because the harness requires every PRD requirement to trace to a `BR-###` ID in the canonical BRD. | answered | No v2 work now. Keep BRD v1 and HTML prototype v1 untouched and focus on v1. Derived v1 planning documents may define stable trace aliases to exact BRD v1 sections without changing the source. Applied to `workspace/plan/00-business/idea.md` and `workspace/state.yaml` on 2026-07-26. |
| Q-003 | The locked HTML prototype v1 has no end-of-day batch-entry surface for `FR-007`/`FT-005` and no admin control surface for `FR-024`/`FT-018`. Which additive design references should define these required MVP surfaces? | `/design import` prototype coverage review | Design approval and `/srs-authoring` | Supply or approve additive references for both surfaces. Recommend one owner-app batch-entry flow that produces the same records as live entry, plus one separate authorized admin surface covering scoped config, flags, referral audit, support lookup, and metrics. | answered | Approved both recommendations on 2026-07-27. Applied as SCR-013 owner batch entry and SCR-014 authorized admin controls across the design system, screen specs, prototype, design README, and state. |
| Q-004 | The locked HTML prototype v1 shows due amounts and attributed income outside the owner-PIN screen, while locked BRD v1 Law 2 and `FR-014` protect due-sensitive and owner-private money values. Which privacy behavior is authoritative? | `/design import` privacy review | Design approval and `/srs-authoring` | A: mask protected values and require the owner PIN before opening due, bill-due, and ROI amounts; B: revise the locked requirement and source set. Recommend A because it preserves the approved shared-device privacy law while keeping non-money job details visible. | answered | Approved option A on 2026-07-27. Due, bill-due, reminder ROI, and attributed-income values are masked outside an owner-PIN-unlocked context. Protected routes and amount reveals require the owner PIN. Non-money job details remain visible. Applied across design v1, the design README, and state. |
| Q-005 | What exact owner-PIN relock, failed-attempt cooldown, and recovery behavior should the SRS require on a shared workshop phone? | `/srs-authoring` security atomicity review | SRS approval | A: relock immediately on explicit lock, leaving a protected route, or app background; relock after 5 minutes of inactivity; after each 5-invalid-PIN failure cycle apply progressive cooldown; recover through registered-phone OTP. B: provide different quantified values. Recommend A with a capped 60/120/240-second progression because it is secure without making rush-hour recovery difficult. | answered | Approved on 2026-07-29: immediate relock on explicit lock, protected-route exit, or background; 5-minute inactivity relock; five invalid entries per failure cycle; cooldowns of 60 seconds, then 120 seconds, then 240 seconds capped; valid registered-phone OTP allows a new PIN; invalid or expired OTP changes nothing; successful PIN or OTP recovery resets escalation. Applied to SRS v1 and its derived feature list/glossary. |
| Q-006 | For the month-4 KPI, when does a workshop count as “requiring support”? | `/srs-authoring` KPI measurability review | NFR-ADOPTION-02 implementation and verification | A: count a workshop once if it needs any human intervention after initial onboarding to complete a core workflow; exclude planned research interviews and product-wide outage notices. B: provide a different measurable definition. Recommend A because it measures product self-sufficiency without treating research contact or mass incidents as usability failure. | answered | The project owner approved SRS v1 on 2026-07-29 while explicitly deferring this definition. NFR-ADOPTION-02 is frozen until an approved SRS amendment resolves it. The accepted deferral is tracked by D-001 during traceability and does not authorize invented measurement behavior. |
| Q-007 | (= `OQ-E01-2`) ADR-0007 defers exact token storage, session lifetime, PIN hashing parameters, Firebase SDK versions and secure-storage choice to a later security contract. What are they? | E01 specification (T01/T02/T03/T05/T06) | E01-T03, T05, T06 | A: adopt conservative published defaults now and revisit before the pilot; B: wait for an owner-authored security contract. Recommend A — B stops all E01 backend work and the defaults below are all standard, revocable choices. | answered (autonomous default) | **A, decided 2026-08-06 under the owner's standing autonomous-run authorization.** PIN verifier: Argon2id via `@node-rs/argon2` (prebuilt, no node-gyp in the Alpine images), m=19456 KiB, t=2, p=1, 16-byte random salt, 32-byte output — the OWASP second-choice minimum. Application session: opaque 256-bit random token, stored only as a SHA-256 hash, 30-day absolute expiry sliding on use, revocable per session row; no JWT, so revocation is immediate. Owner-PIN grant: a separate short-lived grant row, 5-minute inactivity expiry per Q-005, revoked immediately on explicit lock, protected-route exit or app background. Mobile secure storage: `flutter_secure_storage` ^9 (iOS Keychain / Android EncryptedSharedPreferences). Firebase: `firebase_core` + `firebase_auth`, exact versions pinned to the latest stable resolved at implementation time and recorded in the task's completion report, always behind `PhoneIdentityPort` with the fake adapter as the default in dev/test/CI. **Revisit before the pilot** — these are safe defaults, not an owner-approved security contract. |
| Q-008 | (= `OQ-E01-3`) SCR-001 requires approved vehicle types but the SRS enumerates no stable stored keys. Which keys and Bangla/English labels? | E01 specification (T01/T07) | E01-T01 contract enum, E01-T07 chip step | A: use exactly the four vehicle classes BRD v1 names; B: owner supplies a different list. Recommend A because it invents nothing — the BRD already names the set and the beachhead order. | answered (autonomous default) | **A, decided 2026-08-06 under the standing authorization.** Four stable keys, from BRD v1 §1 ("bikes, cars, CNGs and trucks") in the beachhead order of §3: `bike` (মোটরসাইকেল / Motorcycle), `cng` (সিএনজি / CNG), `car` (গাড়ি / Car), `truck` (ট্রাক / Truck). Multi-select, at least one required. No `other` key — that would be invented scope. The enum is additive-only: later keys append, existing keys never change meaning. |
| Q-009 | Q-005 quantifies owner-PIN attempt throttling but says nothing about throttling the OTP *recovery* path, so PIN cooldown could be bypassed by repeatedly recovering. What limits apply? | E01-T05 specification review (raised by team-lead) | E01-T05 recovery path | A: throttle recovery explicitly; B: rely only on the provider's own OTP limits. Recommend A — B leaves the documented cooldown bypassable and the gap would surface in production. | answered (autonomous default) | **A, decided 2026-08-06 under the standing authorization.** Recovery OTP: at most 5 send requests per registered phone per rolling hour; each OTP is valid 5 minutes; at most 5 verify attempts per OTP, after which it is invalidated and a new send is required. Exceeding either limit returns the same redacted `429` envelope with `retryAfterSeconds` and never reveals whether the phone is registered. These sit on top of, not instead of, the provider's limits. Same pilot-revisit caveat as Q-007. |
| Q-010 | ADR-0007 requires the Flutter flow to disclose Google's processing of the phone number and obtain consent, but no approved Bangla/English copy for that disclosure exists in the design canon or the SRS. | E01-T06 specification review (raised by team-lead) | E01-T06 ARB keys, E01-T07 onboarding step | A: author plain-language consent copy now and mark it provisional; B: block T06 until legal/owner copy exists. Recommend A — B blocks the whole mobile lane, and a truthful plain disclosure is better than none. | answered (autonomous default) | **A, decided 2026-08-06 under the standing authorization.** T06 authors the ARB keys with this provisional copy: EN — "To confirm this is your phone, Google sends the code and checks the number. Garazo does not share your workshop records with Google." BN — "এটি আপনার ফোন কিনা নিশ্চিত করতে Google কোড পাঠায় এবং নম্বরটি যাচাই করে। Garazo আপনার ওয়ার্কশপের তথ্য Google-এর সাথে শেয়ার করে না।" Consent is explicit and blocking: onboarding cannot proceed until it is accepted. Marked **provisional — owner/legal must confirm the wording before the pilot**, and the ARB keys are named so replacing the copy touches no code. |

Rules:
- Every question carries a recommendation; agents propose, humans decide.
- When answered, apply the answer to every artifact that raised it in the same
  commit, mark it `answered`, and note where it was applied.
- Batch non-blocking questions for the next checkpoint; never interrupt for
  them.
